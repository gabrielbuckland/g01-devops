/**
 * Represents the data structure for a form submission.
 * This model maps directly to the `Form` entity in the backend.
 */
export interface FormModel {
	email: string;
	vorname: string;
	nachname: string;
}
